<nav class="navbar navbar-expand-lg navbar-light bg-primary  <%= current_user&.admin? ? 'text-white bg-primary' : '' %>">

  <ul class="navbar-nav">
      
      <li class="nav-item nav-link active me-3">
        <%= link_to 'Gists', admin_gists_path, class: 'btn btn-outline-light' %>
      </li>
      <li class="nav-item nav-link active me-3">
        <%= link_to 'Badges', admin_badges_path, class: 'btn btn-outline-light' %>
      </li>
    </ul>

  <div class="container">
    <%= link_to t('helpers.testguru'), current_user&.admin? ? admin_tests_path : root_path, class: current_user&.admin? ? 'navbar-brand text-white' : 'navbar-brand' %>
  </div>

  <div class="nav-admin user">
    <%= t('shared.header.welcome') %> <b class="nav-admin user-email"><%= current_user.email %></b> <%= t('shared.header.guru') %> |
    <%= link_to t('helpers.logout'), destroy_user_session_path, method: :delete, class: 'link-dark' %>
  </div>
</nav>
