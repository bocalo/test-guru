<div class="nav nav-admin">
  <span class="nav-admin title"><%= t('layouts.admin.admin_title') %></span>

  <div class="nav-admin user">
    <%= t('shared.header.welcome') %> <b class="nav-admin user-email"><%= current_user.email %></b> <%= t('shared.header.guru') %> |
    <%= link_to t('helpers.logout'), destroy_user_session_path, method: :delete %>
  </div>
</div>
