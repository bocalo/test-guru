<h1><%= @question.test.title %> <%= t('.header') %></h1>

<p><%= @question.body %></p>

<h2><%= t('.answers') %></h2>
<%= link_to 'New answer', new_admin_question_answer_path(@question) %>
<%= link_to t('helpers.submit.answer.new'), new_admin_question_answer_path(@question) %>
<ol>
  <%= render partial: 'answers/answer', collection: @question.answers %>
</ol>

<%= link_to t('helpers.submit.question.edit'), edit_admin_question_path(@question) %> |
<%= link_to t('helpers.page.back'), admin_test_path(@question.test) %> 
