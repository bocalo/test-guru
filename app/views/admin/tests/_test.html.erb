<tr>
  <td><%= test.title %></td>
  <td><%= test_level(test) %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to t('helpers.show'), admin_test_path(test) %></td>
  <td><%= link_to t('helpers.edit'), edit_admin_test_path(test) %></td>
  <td>
    <%= link_to t('helpers.delete'),
                admin_test_path(test),
                method: :delete,
                data: { confirm: t('helpers.sure')} %>
  </td>
<tr>
