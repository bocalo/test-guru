<div class="row justify-content-center mt-3">
  <div class="col col-md-4">
    <h1><%= t('.header') %></h1>
    <p>
      <%= @answer.question.body %>
    </p>
    <%= render 'form' %>
    <div class="mt-3">
      <%= link_to t('helpers.page'), admin_question_path(@answer.question), class: 'btn btn-outline-primary' %>
    </div>
  </div>
</div>
