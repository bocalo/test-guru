<div class="row justify-content-center mt-3">
  <div class="col col-md-4">
    <h1><%= t('.header') %></h1>
    <p>
      <%= @question.body %>
    </p>
    <%= render 'form' %><br>

    <%= link_to t('helpers.page'), admin_question_path(@question), class: 'btn btn-outline-primary' %>
  </div>
</div>
